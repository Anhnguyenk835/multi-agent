[project]
name = "distributed-agent-contracts"
version = "0.1.0"
description = "Shared contracts for the distributed-agent demo"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.10.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
