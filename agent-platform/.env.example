# Direct URL for the browser to reach the Orchestrator API.
# Leave unset to use the local Vite /api proxy instead.
VITE_ORCHESTRATOR_BASE_URL=http://localhost:8000
VITE_BACKEND_BASE_URL=http://localhost:8010/api
